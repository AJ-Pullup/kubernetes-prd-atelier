apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
namespace: dev

configMapGenerator:
- name: app-config
  behavior: replace
  literals:
  - config.json={"message":"It's a dev env. bro !"}

patches:
- path: patches.yaml